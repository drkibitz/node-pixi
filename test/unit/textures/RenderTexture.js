describe('textures/RenderTexture', function () {
    'use strict';

    var RenderTexture = PIXI.RenderTexture;

    it('Module exists', function () {
        expect(RenderTexture).to.be.a('function');
    });
});
