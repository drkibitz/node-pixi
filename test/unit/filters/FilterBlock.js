describe('filters/FilterBlock', function () {
    'use strict';

    var FilterBlock = PIXI.FilterBlock;

    it('Module exists', function () {
        expect(FilterBlock).to.be.a('function');
    });
});
