describe('loaders/JsonLoader', function () {
    'use strict';

    var JsonLoader = PIXI.JsonLoader;

    it('Module exists', function () {
        expect(JsonLoader).to.be.a('function');
    });
});
