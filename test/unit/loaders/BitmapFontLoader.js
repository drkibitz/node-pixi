describe('loaders/BitmapFontLoader', function () {
    'use strict';

    var BitmapFontLoader = PIXI.BitmapFontLoader;

    it('Module exists', function () {
        expect(PIXI.BitmapFontLoader).to.be.a('function');
    });
});
