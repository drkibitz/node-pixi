describe('loaders/SpineLoader', function () {
    'use strict';

    var SpineLoader = PIXI.SpineLoader;

    it('Module exists', function () {
        expect(SpineLoader).to.be.a('function');
    });
});
